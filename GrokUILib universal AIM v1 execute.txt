-- GrokLib by Ic3 Corp Cheats, this Script is not 100% segured to use, riskBan is low.

local lib = loadstring(game:HttpGet("SUA_URL_AQUI"))()

-- Create a window for UI
local window = lib:CreateWindow("GrokLib - Ic3 Corp")

-- Add a button to execute custom toast
window:AddButton("Mostrar Notificação", function()
    window:AddNotification(
        "Bem-vindo!",
        "Universal Aimbot foi executado com sucesso!",
        5, -- Duração (segundos)
        { -- Cores personalizadas
            Background = Color3.fromRGB(40, 40, 40),
            Title = Color3.fromRGB(0, 255, 128),
            Description = Color3.fromRGB(180, 180, 180)
        }
    )
end)

-- Add a toggle show the state 
window:AddToggle("Ativar Modo", function(state)
    window:AddNotification("Toggle", state and "Ativado!" or "Desativado!", 3)
end)

-- Add a slider show the value
window:AddSlider("Velocidade", 1, 100, 50, function(value)
    window:AddNotification("Slider", "Valor: " .. value, 2, {
        Background = Color3.fromRGB(50, 0, 100),
        Title = Color3.fromRGB(255, 255, 0),
        Description = Color3.fromRGB(200, 200, 200)
    })
end)

-- Notificação inicial ao carregar o script
window:AddNotification("Carregado!", "GrokLib da Ic3 Corp esta ativa e executada!", 4)